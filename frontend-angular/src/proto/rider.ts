// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.32.0
// source: rider.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "lastmile.rider";

export interface RegisterRideIntentRequest {
  userId: string;
  stationAreaId: string;
  arrivalTime: Date | undefined;
  destinationAreaId: string;
  partySize: number;
}

export interface RideIntentResponse {
  intentId: string;
}

export interface RideId {
  id: string;
}

export interface RideStatus {
  intentId: string;
  status: RideStatus_Status;
  tripId: string;
  destinationAreaId: string;
  stationAreaId: string;
  arrivalTime: Date | undefined;
}

export enum RideStatus_Status {
  PENDING = 0,
  SCHEDULED = 1,
  PICKED_UP = 2,
  COMPLETED = 3,
  CANCELLED = 4,
  UNRECOGNIZED = -1,
}

export function rideStatus_StatusFromJSON(object: any): RideStatus_Status {
  switch (object) {
    case 0:
    case "PENDING":
      return RideStatus_Status.PENDING;
    case 1:
    case "SCHEDULED":
      return RideStatus_Status.SCHEDULED;
    case 2:
    case "PICKED_UP":
      return RideStatus_Status.PICKED_UP;
    case 3:
    case "COMPLETED":
      return RideStatus_Status.COMPLETED;
    case 4:
    case "CANCELLED":
      return RideStatus_Status.CANCELLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RideStatus_Status.UNRECOGNIZED;
  }
}

export function rideStatus_StatusToJSON(object: RideStatus_Status): string {
  switch (object) {
    case RideStatus_Status.PENDING:
      return "PENDING";
    case RideStatus_Status.SCHEDULED:
      return "SCHEDULED";
    case RideStatus_Status.PICKED_UP:
      return "PICKED_UP";
    case RideStatus_Status.COMPLETED:
      return "COMPLETED";
    case RideStatus_Status.CANCELLED:
      return "CANCELLED";
    case RideStatus_Status.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface GetRideHistoryRequest {
  userId: string;
}

export interface RideHistoryResponse {
  rides: RideStatus[];
}

function createBaseRegisterRideIntentRequest(): RegisterRideIntentRequest {
  return { userId: "", stationAreaId: "", arrivalTime: undefined, destinationAreaId: "", partySize: 0 };
}

export const RegisterRideIntentRequest: MessageFns<RegisterRideIntentRequest> = {
  encode(message: RegisterRideIntentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.stationAreaId !== "") {
      writer.uint32(18).string(message.stationAreaId);
    }
    if (message.arrivalTime !== undefined) {
      Timestamp.encode(toTimestamp(message.arrivalTime), writer.uint32(26).fork()).join();
    }
    if (message.destinationAreaId !== "") {
      writer.uint32(34).string(message.destinationAreaId);
    }
    if (message.partySize !== 0) {
      writer.uint32(40).int32(message.partySize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterRideIntentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterRideIntentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stationAreaId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.arrivalTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.destinationAreaId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.partySize = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterRideIntentRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      stationAreaId: isSet(object.stationAreaId) ? globalThis.String(object.stationAreaId) : "",
      arrivalTime: isSet(object.arrivalTime) ? fromJsonTimestamp(object.arrivalTime) : undefined,
      destinationAreaId: isSet(object.destinationAreaId) ? globalThis.String(object.destinationAreaId) : "",
      partySize: isSet(object.partySize) ? globalThis.Number(object.partySize) : 0,
    };
  },

  toJSON(message: RegisterRideIntentRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.stationAreaId !== "") {
      obj.stationAreaId = message.stationAreaId;
    }
    if (message.arrivalTime !== undefined) {
      obj.arrivalTime = message.arrivalTime.toISOString();
    }
    if (message.destinationAreaId !== "") {
      obj.destinationAreaId = message.destinationAreaId;
    }
    if (message.partySize !== 0) {
      obj.partySize = Math.round(message.partySize);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterRideIntentRequest>, I>>(base?: I): RegisterRideIntentRequest {
    return RegisterRideIntentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterRideIntentRequest>, I>>(object: I): RegisterRideIntentRequest {
    const message = createBaseRegisterRideIntentRequest();
    message.userId = object.userId ?? "";
    message.stationAreaId = object.stationAreaId ?? "";
    message.arrivalTime = object.arrivalTime ?? undefined;
    message.destinationAreaId = object.destinationAreaId ?? "";
    message.partySize = object.partySize ?? 0;
    return message;
  },
};

function createBaseRideIntentResponse(): RideIntentResponse {
  return { intentId: "" };
}

export const RideIntentResponse: MessageFns<RideIntentResponse> = {
  encode(message: RideIntentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.intentId !== "") {
      writer.uint32(10).string(message.intentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RideIntentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRideIntentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.intentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RideIntentResponse {
    return { intentId: isSet(object.intentId) ? globalThis.String(object.intentId) : "" };
  },

  toJSON(message: RideIntentResponse): unknown {
    const obj: any = {};
    if (message.intentId !== "") {
      obj.intentId = message.intentId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RideIntentResponse>, I>>(base?: I): RideIntentResponse {
    return RideIntentResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RideIntentResponse>, I>>(object: I): RideIntentResponse {
    const message = createBaseRideIntentResponse();
    message.intentId = object.intentId ?? "";
    return message;
  },
};

function createBaseRideId(): RideId {
  return { id: "" };
}

export const RideId: MessageFns<RideId> = {
  encode(message: RideId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RideId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRideId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RideId {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: RideId): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RideId>, I>>(base?: I): RideId {
    return RideId.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RideId>, I>>(object: I): RideId {
    const message = createBaseRideId();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseRideStatus(): RideStatus {
  return { intentId: "", status: 0, tripId: "", destinationAreaId: "", stationAreaId: "", arrivalTime: undefined };
}

export const RideStatus: MessageFns<RideStatus> = {
  encode(message: RideStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.intentId !== "") {
      writer.uint32(10).string(message.intentId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.tripId !== "") {
      writer.uint32(26).string(message.tripId);
    }
    if (message.destinationAreaId !== "") {
      writer.uint32(34).string(message.destinationAreaId);
    }
    if (message.stationAreaId !== "") {
      writer.uint32(42).string(message.stationAreaId);
    }
    if (message.arrivalTime !== undefined) {
      Timestamp.encode(toTimestamp(message.arrivalTime), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RideStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRideStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.intentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.destinationAreaId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.stationAreaId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.arrivalTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RideStatus {
    return {
      intentId: isSet(object.intentId) ? globalThis.String(object.intentId) : "",
      status: isSet(object.status) ? rideStatus_StatusFromJSON(object.status) : 0,
      tripId: isSet(object.tripId) ? globalThis.String(object.tripId) : "",
      destinationAreaId: isSet(object.destinationAreaId) ? globalThis.String(object.destinationAreaId) : "",
      stationAreaId: isSet(object.stationAreaId) ? globalThis.String(object.stationAreaId) : "",
      arrivalTime: isSet(object.arrivalTime) ? fromJsonTimestamp(object.arrivalTime) : undefined,
    };
  },

  toJSON(message: RideStatus): unknown {
    const obj: any = {};
    if (message.intentId !== "") {
      obj.intentId = message.intentId;
    }
    if (message.status !== 0) {
      obj.status = rideStatus_StatusToJSON(message.status);
    }
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    if (message.destinationAreaId !== "") {
      obj.destinationAreaId = message.destinationAreaId;
    }
    if (message.stationAreaId !== "") {
      obj.stationAreaId = message.stationAreaId;
    }
    if (message.arrivalTime !== undefined) {
      obj.arrivalTime = message.arrivalTime.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RideStatus>, I>>(base?: I): RideStatus {
    return RideStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RideStatus>, I>>(object: I): RideStatus {
    const message = createBaseRideStatus();
    message.intentId = object.intentId ?? "";
    message.status = object.status ?? 0;
    message.tripId = object.tripId ?? "";
    message.destinationAreaId = object.destinationAreaId ?? "";
    message.stationAreaId = object.stationAreaId ?? "";
    message.arrivalTime = object.arrivalTime ?? undefined;
    return message;
  },
};

function createBaseGetRideHistoryRequest(): GetRideHistoryRequest {
  return { userId: "" };
}

export const GetRideHistoryRequest: MessageFns<GetRideHistoryRequest> = {
  encode(message: GetRideHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRideHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRideHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRideHistoryRequest {
    return { userId: isSet(object.userId) ? globalThis.String(object.userId) : "" };
  },

  toJSON(message: GetRideHistoryRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetRideHistoryRequest>, I>>(base?: I): GetRideHistoryRequest {
    return GetRideHistoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetRideHistoryRequest>, I>>(object: I): GetRideHistoryRequest {
    const message = createBaseGetRideHistoryRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseRideHistoryResponse(): RideHistoryResponse {
  return { rides: [] };
}

export const RideHistoryResponse: MessageFns<RideHistoryResponse> = {
  encode(message: RideHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rides) {
      RideStatus.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RideHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRideHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rides.push(RideStatus.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RideHistoryResponse {
    return {
      rides: globalThis.Array.isArray(object?.rides) ? object.rides.map((e: any) => RideStatus.fromJSON(e)) : [],
    };
  },

  toJSON(message: RideHistoryResponse): unknown {
    const obj: any = {};
    if (message.rides?.length) {
      obj.rides = message.rides.map((e) => RideStatus.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RideHistoryResponse>, I>>(base?: I): RideHistoryResponse {
    return RideHistoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RideHistoryResponse>, I>>(object: I): RideHistoryResponse {
    const message = createBaseRideHistoryResponse();
    message.rides = object.rides?.map((e) => RideStatus.fromPartial(e)) || [];
    return message;
  },
};

export interface RiderService {
  RegisterRideIntent(request: RegisterRideIntentRequest): Promise<RideIntentResponse>;
  GetRideStatus(request: RideId): Promise<RideStatus>;
  GetRideHistory(request: GetRideHistoryRequest): Promise<RideHistoryResponse>;
}

export const RiderServiceServiceName = "lastmile.rider.RiderService";
export class RiderServiceClientImpl implements RiderService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || RiderServiceServiceName;
    this.rpc = rpc;
    this.RegisterRideIntent = this.RegisterRideIntent.bind(this);
    this.GetRideStatus = this.GetRideStatus.bind(this);
    this.GetRideHistory = this.GetRideHistory.bind(this);
  }
  RegisterRideIntent(request: RegisterRideIntentRequest): Promise<RideIntentResponse> {
    const data = RegisterRideIntentRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RegisterRideIntent", data);
    return promise.then((data) => RideIntentResponse.decode(new BinaryReader(data)));
  }

  GetRideStatus(request: RideId): Promise<RideStatus> {
    const data = RideId.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetRideStatus", data);
    return promise.then((data) => RideStatus.decode(new BinaryReader(data)));
  }

  GetRideHistory(request: GetRideHistoryRequest): Promise<RideHistoryResponse> {
    const data = GetRideHistoryRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetRideHistory", data);
    return promise.then((data) => RideHistoryResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
